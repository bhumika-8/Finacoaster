<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spent History</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRvRQpT01p7f2K/gAsIlUlfSzN4TQU5lyzICfv28M"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        /* General Styling */
        body {
            background-color: #121212; /* Deep dark theme */
            color: #ffffff; /* White text for readability */
            font-family: 'Arial', sans-serif;
        }
s
        /* Heading Styling */
        h1 {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 2rem;
            color: white
        }

        /* Table Styling */
        .table {
            width: 100%;
            border-collapse: collapse;
            background-color: transparent;
        }

        .table thead th {
            border-bottom: 0.2vh solid rgba(0, 128, 0, 0.6) ; /* Olive green lines for table header */
            text-align: center;
            font-size: 1.2rem;
            color: rgba(0, 128, 0, 0.6);
        }

        .table tbody tr td {
            border-bottom: 0.2vh solid rgba(0, 128, 0, 0.6); /* Olive green lines for rows */
            text-align: center;
            padding: 0.75rem;
            font-size: 1rem;
            color: #e0e0e0; /* Subtle light gray */
        }
        .header-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}

/* Logo Styling */
.logo {
  font-size: 1.5rem;
  font-weight: bold;
  text-decoration: none;
  color: rgba(0, 128, 0, 0.8); /* Olive green for the logo */
}

/* Navigation Menu */
nav ul {
  list-style-type: none;
  display: flex;
}

nav ul li {
  margin-left: 20px;
}

nav ul li a {
  text-decoration: none;
  color: white;
  font-size: 1rem;
  transition: color 0.3s ease;
}

nav ul li a:hover {
  color: rgba(0, 128, 0, 0.8); /* Olive green on hover */
}
        /* Table Responsive Styling */
        @media (max-width: 768px) {
            .table {
                font-size: 0.9rem; /* Adjust font size for smaller screens */
            }
        }

        /* Table Container */
        .table-container {
            max-width: 1200px; /* Center the table within a container */
            margin: auto;
            padding: 0 1rem; /* Add slight padding for mobile responsiveness */
        }

        /* No Bright Shadows or Glow */
        th, td {
            box-shadow: none; /* Remove any bright shadow effects */
        }
    </style>
</head>
<body>
    <header>
        <div class="header-container">
          <a href="/" class="logo">Finacoaster</a>
          <nav>
            <ul>
              
              <li><i class="fa-solid fa-house"></i><a href="/es"> Dashboard</a></li>
            </ul>
          </nav>
        </div>
      </header>
    <div class="container my-5">
        <h1>Spent History for <%= username %></h1>
        <div class="table-container">
            <table class="table">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Amount ($)</th>
                        <th>Timestamp</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (expense.length === 0) { %>
                        <tr>
                            <td colspan="3" class="text-center">No spend history found</td>
                        </tr>
                    <% } else { %>
                        <% expense.forEach((item, index) => { %>
                            <tr>
                                <td> <%= index + 1 %> </td>
                                <td> <%= item.amount %> </td>
                                <td> <%= new Date(item.timestamp).toLocaleString() %> </td>
                            </tr>
                        <% }); %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
    <script src="/docs/5.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>

